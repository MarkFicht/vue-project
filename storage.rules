rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default-deny until explicit upload/download paths are introduced.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
